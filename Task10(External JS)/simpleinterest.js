 // SI = Simple Interest
      // P = Principal Amount
      // R = Rate of Interest
      // T Time Period

      function Simple_Interest(P, R, T) {
        let SI = (P * R * T) / 100;
        console.log(SI);
      }