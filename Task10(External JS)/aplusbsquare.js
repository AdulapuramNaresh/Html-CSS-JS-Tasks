// A = First Number
      // B = Second Number
      // Square = Whole Square of the two numbers

      function A_plus_B_Square(A, B) {
        let Square = (A + B) * (A + B);
        console.log(Square);
      }