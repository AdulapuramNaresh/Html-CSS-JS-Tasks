   // a = First Number
      // b = Second Number
      // c = Third Number
      // Avg = Average of the three numbers

      function Average_of_three(a, b, c) {
        let Avg = (a + b + c) / 3;
        console.log(Avg);
      }