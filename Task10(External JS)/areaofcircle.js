//  Area = Area of Circle
      // pi = Constant(3.14)
      // radius = radius of the circle

      function Areaofcircle(radius) {
        const pi = 3.14;
        let Area = pi * radius * radius;
        console.log(Area);
      }